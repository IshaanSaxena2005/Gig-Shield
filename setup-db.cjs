console.log('Gig-Shield no longer requires MySQL setup for local development.')
console.log('The backend now uses SQLite at backend/database.sqlite.')
console.log('Start the backend with `npm --prefix backend run dev` and the schema will be initialized automatically.')
